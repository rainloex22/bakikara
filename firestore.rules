rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // 1. MÜŞTERİ HESAPLARI (users koleksiyonu)
    match /users/{userId} {
      
      // 🔥 ÇÖZÜM: Oturum açmış herkesin (admin dahil) tüm kullanıcıları okumasına izin ver.
      // Bu kural, admin panelindeki tüm listeleme/sorgulama isteklerini (400 hatası) çözecektir.
      allow read: if request.auth != null; 

      // Kendi belgesini güncelleme/silme iznini koru.
      allow update, delete: if request.auth != null && request.auth.uid == userId;
      
      // Giriş yapmış herkes hesap oluşturabilir.
      allow create: if request.auth != null;
    }
    
    // ... (Diğer kurallar)

    match /{document=**} {
        allow read, write: if false;
    }
  }
}
